drop table registrations;

CREATE TABLE registrations (
   id serial PRIMARY KEY,
   name VARCHAR (255),
   email VARCHAR (255),
   message TEXT,
   hike_uuid VARCHAR(255) NOT NULL,
   date_added DATE
);

